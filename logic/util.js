function Util() {

    function calculateTotalDistributedPassengers() {

        let totalPassengerAmount
        new PassengerAmount = distributeAllSeatsToAllPassengers();

        for (totalPassengerAmount of PassengerAmount)
        totalPassengerAmount += PassengerAmount

        return totalPassengerAmount;
    }

    return calculateTotalDistributedPassengers;
}

module.exports = Util();